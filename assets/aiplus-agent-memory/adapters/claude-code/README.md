# Claude Code Adapter

Claude Code integration is project-local. Use project commands and prompts that
call:

```bash
aiplus memory context --runtime claude-code --budget 2000
aiplus identity context --role advisor
```

Do not edit `~/.claude` or other global configuration.
