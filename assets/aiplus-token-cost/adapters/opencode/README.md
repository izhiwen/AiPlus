# OpenCode Adapter Note

Token-cost has no OpenCode-specific prompt payload. The shared entry point is:

```bash
aiplus agent token-cost
```

AiPlus release archives also install `aiplus-token-cost` for standalone use.
