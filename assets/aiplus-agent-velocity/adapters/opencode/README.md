# AiPlus-Agent-Velocity — opencode Adapter

Placeholder for OpenCode runtime adapter.

For v0.1, velocity tracking is invoked via `aiplus velocity init / estimate / complete / report` directly and works under any runtime without runtime-specific integration. v0.2 may add an OpenCode agent / command that calls `aiplus velocity` automatically at task boundaries.
