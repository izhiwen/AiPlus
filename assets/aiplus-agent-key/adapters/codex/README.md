# AiPlus-Agent-Key — codex Adapter

Placeholder for codex runtime adapter.

For v0.1, the broker is invoked via `aiplus secret-broker run --aliases <list> -- <command>` and works under any runtime without runtime-specific integration. v0.2 may add a Codex skill that calls the broker automatically for declared aliases at session start.
