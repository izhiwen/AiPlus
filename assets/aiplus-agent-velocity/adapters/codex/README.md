# AiPlus-Agent-Velocity — codex Adapter

Placeholder for codex runtime adapter.

For v0.1, velocity tracking is invoked via `aiplus velocity init / estimate / complete / report` directly and works under any runtime without runtime-specific integration. v0.2 may add a Codex skill that calls `aiplus velocity estimate` automatically at task start and `aiplus velocity complete` on `agent route` completion.
