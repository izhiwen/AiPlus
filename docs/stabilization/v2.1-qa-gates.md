# v2.1 QA Gates

**Version:** v2.1  
**Status:** Planning

---

## QA Checklist for Each v2.1 Task

### Code Quality (All Tasks)

| Check | Command | Gate |
|-------|---------|------|
| Format | `cargo fmt --all --check` | Must PASS |
| Clippy | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Must PASS |
| Tests | `cargo test --workspace` | Must PASS (≥116 tests) |
| New Tests | Task-specific unit tests | Must exist and PASS |

### Runtime QA (All Tasks)

| Check | Gate |
|-------|------|
| `aiplus doctor` | PASS |
| `aiplus memory doctor` | PASS |
| `aiplus profile doctor` | PASS |
| Task-specific smoke | Defined per task card |

### Boundary QA (All Tasks)

| Check | Gate |
|-------|------|
| Secret scan | No new secret patterns |
| Private content scan | No private profile content in public assets |
| Telemetry scan | No telemetry added |
| Global config scan | No global config edits |

### Task-Specific QA

#### compact-resume-capsule-integration
- `cargo run -p aiplus-cli -- compact resume --dry-run` shows capsule read
- `cargo run -p aiplus-cli -- compact validate` still PASS
- Fallback to handoff.md works when capsule missing

#### decision-ledger-extraction
- `cargo run -p aiplus-cli -- compact prepare` creates capsule with decisions
- Decisions array is non-empty when decision-log.md has entries
- No secret/private content in extracted decisions

#### memory-doctor-deep-scan
- `cargo run -p aiplus-cli -- memory doctor` shows record scan summary
- Dry-run mode does not modify records
- Rejected/forgotten records reported separately

#### profile-bundle-schema-validation
- `cargo run -p aiplus-cli -- profile doctor aiplus-work-with-zhiwen` validates all files
- Invalid identity files flagged as NEEDS_FIX
- Missing bundle files reported correctly

#### installed-binary-upgrade-path
- Docs clearly explain upgrade steps
- Local smoke test confirms upgrade works
- No duplicate managed blocks created

---

**QA Owner:** Runtime QA Agent  
**Sign-off Required:** Before any v2.1 task marked complete
