# AiPlus-Agent-Key — claude-code Adapter

Placeholder for claude-code runtime adapter.

For v0.1, the broker is invoked via `aiplus secret-broker run --aliases <list> -- <command>` and works under any runtime without runtime-specific integration. v0.2 may add a Claude Code project-local skill that wraps `secret-broker run` for declared aliases.
