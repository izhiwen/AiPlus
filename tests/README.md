# AiPlus Rust QA Tests

The executable Rust integration tests live in `crates/aiplus-cli/tests/` so
`cargo test` runs them for the `aiplus-cli` package.

This directory is reserved for cross-package QA notes, fixtures, and dogfood
evidence that apply to the Rust workspace as a whole.
