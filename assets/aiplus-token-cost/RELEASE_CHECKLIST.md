# aiplus-token-cost Bundle Release Checklist

- [ ] Standalone `izhiwen/AiPlus-Token-Cost` release exists for the target tag.
- [ ] AiPlus release archives contain `aiplus`.
- [ ] AiPlus release archives contain `aiplus-token-cost`.
- [ ] `install.sh` installs both Unix binaries when present.
- [ ] `install.ps1` installs both Windows binaries when present.
- [ ] `aiplus agent token-cost` still runs through the bundled CLI.
- [ ] `aiplus doctor` surfaces token-cost binary PATH status as INFO.
- [ ] No telemetry or secret material is bundled.
