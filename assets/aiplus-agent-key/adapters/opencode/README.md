# AiPlus-Agent-Key — opencode Adapter

Placeholder for opencode runtime adapter.

For v0.1, the broker is invoked via `aiplus secret-broker run --aliases <list> -- <command>` and works under any runtime without runtime-specific integration. v0.2 may add an OpenCode project-local command that wraps `secret-broker run` for declared aliases.
