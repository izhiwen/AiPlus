# AiPlus-Agent-Velocity — claude-code Adapter

Placeholder for Claude Code runtime adapter.

For v0.1, velocity tracking is invoked via `aiplus velocity init / estimate / complete / report` directly and works under any runtime without runtime-specific integration. v0.2 may add a Claude Code slash command (`/aiplus-velocity-estimate`) that wraps `aiplus velocity estimate`.
